# 戦略指南 AI（AB3C Analyzer）開発ガイド

> **必読**: セッション開始時に `docs/AB3C入門ブック.pdf` を必ず読むこと。AB3Cフレームワークの公式解説書であり、分析ロジックの基盤となる知識が記載されている。

## プロジェクト概要
「戦略指南 AI」は、権成俊氏が20年以上のコンサルティング実績から開発したAB3Cフレームワーク（Advantage・Benefit・Customer・Competitor・Company）をSaaS化した戦略策定ツール。非エンジニアながらClaudeと協働開発しており、日本の中小企業に戦略コンサルティング品質の分析を届けることをミッションとしている。

### 本質的差別化（重要）
他のフレームワーク系ツール（3C・4P・5フォース等のAI自動分析）と決定的に違うのは、**「分析→新事業プラン作成→アクション実行」までの一気通貫フロー**を提供すること。「分析しっぱなし」では満足度が出ない。権さん自身が成果を出している実フローをそのまま実装している。
**自己分析（経営者の価値観・原体験の言語化）はサービス体験の中核**。本人の気づき＝アイデンティティ確立＝自信＝戦略を堂々と打ち出す力、という連鎖を支援する。

- **本番URL**: https://senryaku.ai （旧: analyzer.ab3c.jp）
- **Vercelプロジェクト**: ngon-alts-projects/ab3c-analyzer
- **リポジトリ**: ngon-alt/AB3C
- **主なステークホルダー**: FutureShop（ECプラットフォーム、約2,500社が加盟）との提携試験中

## 技術スタック
```
フロントエンド: Next.js 14.2.5 / React 18 / Client Components ("use client")
認証: NextAuth.js 4.24.0（Google OAuth）
DB: Neon PostgreSQL（users, tickets テーブル）
決済: Stripe（ライブキー sk_live_、Vercel環境変数に設定）
AI: Anthropic Claude API（claude-sonnet-4-6）
メール: Resend（FROM: info@senryaku.ai）
ホスティング: Vercel（GitHub mainブランチ自動デプロイ）
フォント: Noto Serif JP（Google Fonts経由）
```

## AB3Cフレームワークの理解（重要）

### 基本構造
AB3C分析は「選ばれる理由」を明らかにする事業戦略フレームワーク：
- **B (Benefit)**: お客様が求める価値（ニーズ→ウォンツ）
- **A (Advantage)**: 差別的優位点・好ましい違い（競合より選ばれる理由）
- **3C**: Customer（顧客）・Competitor（競合）・Company（自社）
- **戦略メッセージ**: Benefit + Advantage の統合表現
- **チェックポイント**: AB3Cの整合性を5項目で評価（ok/warn/ng）

### AB3Cフレームワーク原則（コース全7回学習済み）
1. **ビジョンから始めよ**: 大きな石から入れる、ビジョン→戦略→戦術の順
2. **戦略とは**: 問題の本質を見極める
3. **AB3Cで戦略を表現**: 全体と部分を一緒に表現、BenefitとAdvantageをデザインに直結、一気通貫
4. **調査分析**: レビュー・検索KW・VRIO分析でAB3C全要素を調査
5. **商品・サービス企画**: AB3C不成立時は商品開発を提案、価値サークル、業界常識を壊す
6. **コンテンツ企画**: AB3C→ユーザーシナリオ→CJM→コンテンツの順
7. **何者になるか**: 実行できない本質はビジョン欠如、パッション→ポジション→ミッション

### 分析時の重要原則
- ビジョンから問う
- 問題の本質を掘る（表面的課題の奥を問う）
- BenefitとAdvantageを明確に分離
- **一気通貫**（Web・商品・営業・採用すべてに同じ戦略軸）
- 競合視点重視（「好ましい違い」か問う）
- Advantage不成立時は商品・サービス開発を提案
- 経営者の価値観を3層で掘る（外の違い→理由→価値観）
- 業界の常識に疑問を持つ
- **Advantage の根拠を問いかけ型で促す** — 第三者証明は必須ではないが、本人が確信を持って語れる根拠（独自経験・先駆者性・実績等）があるかを確認・言語化を促す。「最高」「一番」のような主観的すぎる主張は本人なりの根拠の整理を促す。スタンスは断定ではなくコーチング型（「〜の根拠を整理しておくと信頼性が高まります」）

### 分析結果JSONスキーマ
```
benefit: { core, needs[], wants[] }
advantage: { what, why_good, why_hard_to_copy }
three_c: {
  customer: { target, profile[], stage, cutoff, market: { sam, som, growth, basis } },
  competitor: { direct[], indirect[] },
  company: { strength[], structure, passion }
}
strategy_message: { message, benefit_part, advantage_part }
checkpoints: [{ label, status, comment }]
```

### AI生成テキストの言語ポリシー（厳守）
- **内部の enum 値（`ok` / `warn` / `ng` 等）はユーザーに直接見せない**。UI 側で「問題なし」「注意」「警告」等の日本語表記に変換する
- **AI が生成する comment / 本文に英語の状態ラベル（`warn`, `[warn]`, `ok`, `ng` 等）を含めない**。評価の度合いは「問題なし」「要注意」「改善が必要」「整っている」「まだ弱い」等の自然な日本語で表現する
- **一般に馴染みの薄い英単語・略語（VRIO, KPI, CVR, CPM 等）は避ける**。使う必要がある場合は日本語で噛み砕いた言い換えを併記（例: 「VRIO分析」→「真似しづらさの分析」）
- 既に広く使われているカタカナ語（ウェブサイト、ターゲット、コンテンツ、ベネフィット、アドバンテージ、ブランド、メッセージ等）は使用可
- プロンプト変更時もこのポリシーを保持すること

## 色のルール（厳守）

### 意味のある色（他の用途に使わない）
| 色 | コード | 用途 |
|---|---|---|
| 赤 | #FF0000 (C.B) | Benefit専用。URL入力タブのボーダーにも使用 |
| 青 | #1a6fd4 (C.A) | Advantage専用。テキスト入力タブのボーダーにも使用 |
| 黒 | #1a1a14 (C.C) | 3C分析・戦略メッセージ背景 |

### フェーズカラー
| フェーズ | 濃い色 | 薄い色（背景） | 用途 |
|---|---|---|---|
| 戦略策定 | #0d9488 (phase1, ティール) | #a7e9e0 (phase1Bg) | サイドバー・チャットヘッダー・ボタン / チャット背景 |
| 戦略アクション | #ea580c (phase2, オレンジ) | #fed7aa (phase2Bg) | サイドバー・チャットヘッダー・ボタン / チャット背景 |

### UIボタン色
- グレー #555: 操作ボタン（新規分析/再分析/シェア/印刷）
- PROバッジ: 青 #1a6fd4
- プランを見る: 赤 #FF0000
- beige #fef3c7 (C.highlight): 入力フィールド限定
- カード背景: #e8e8e8
- 非アクティブタブ: #d0d0d0
- 全テキスト: 純黒 #000000 統一

## フォントサイズルール（絶対厳守・最重要）

### 大原則
**年配の経営者・伴走支援者が使うサービスのため、文字は大きく。**
**本文は18px、絶対最低は16px。これより小さい文字は原則として禁止。**

### 使用可能なサイズ（これ以外は使わない）
| 用途 | サイズ | フォント |
|---|---|---|
| 大見出し（H1相当） | 28px〜40px | Noto Serif JP |
| セクション見出し（B/A/3C） | 22px〜26px | Noto Serif JP |
| カードタイトル | 22px〜26px | Noto Serif JP or Space Mono |
| 戦略メッセージ本文 | 22px | system-ui |
| 本文・リスト項目・desc | **18px**（必須） | system-ui, サンセリフ体 |
| 改善レポート本文 | 18px | system-ui, サンセリフ体 |
| テーマタブ（伴走） | 18px | system-ui, サンセリフ体 |
| 小ラベル・eyebrow・補足 | **16px**（最低） | system-ui, サンセリフ体 |

### 絶対に使ってはいけないサイズ
**`fontSize: 11`, `12`, `13`, `14`, `15` は新規UIで使用禁止。**

これらが既存コードにあるのは過去の遺物。新規にこのサイズを書くな。
レビュー時に上記サイズを見つけたら必ず16px以上に直すこと。

### 唯一の例外
- **サイドバー幅 200px** のような明確な幅制約がある場所：11〜14px 可（既存実装の継承）
- **印刷・PDFレイアウト** の細密表記：別系統で管理

その他、「ちょっと装飾的に小さくしたい」「タイトルとの差をつけたい」という装飾動機で
小さくするのは**禁止**。差は色・太さ・余白・letterSpacing でつける。

### 実装時のチェックリスト（必ず確認）
- [ ] 新規UIに `fontSize: 11〜15` を書いていないか？
- [ ] 「desc」「補足」「説明」のテキストが 18px か？
- [ ] eyebrow/タグ風ラベルが 16px 以上か？
- [ ] hover state や aria-label でしか出ない文字を除き、ユーザーの目に入る全ての文字が 16px 以上か？

### 違反した場合
- 装飾目的で小文字を使った場合、権さんから繰り返し指摘される。
- 「最低文字サイズを決めて明記してください」と何度も指示されている事実を重く受け止めること。
- 不安なら 18px、迷ったら 18px、見直しても 18px。

### フォント種別
**本文は全てサンセリフ体（ゴシック）**。小さい文字でセリフ体は読みづらいため。
見出しのみ Noto Serif JP / Space Mono 使用可。

### フォント定義（globals.css に CSS 変数として定義済み）
```
--font-body:    system-ui ベース（ゴシック）  ← 本文・リスト・ボタン全部
--font-heading: 'Noto Serif JP', serif（明朝） ← 見出しのみ
--font-mono:    'Space Mono', monospace      ← コード片・英文ラベル
```
- body のデフォルトは `--font-body`（ゴシック）
- h1〜h6 のデフォルトは `--font-heading`（明朝）
- **新規UIで `fontFamily: "'Noto Serif JP', serif"` を直接指定する場合は「これは見出しか」を必ず確認**
- ページ全体の wrapper（`<main>` `<div>`）に `fontFamily: serif` を入れない（サブツリー全体が明朝になる）

## 2フェーズ構造

### 分析フェーズ（STEP 1）
```
[サイドバー200px] [メインコンテンツ 1fr] [分析チャット 400px(fixed)]
  緑の濃い背景     分析結果(ResultView)      緑の薄い背景
  分析履歴一覧     + 改善レポート            AnalysisChatPanel
                   💬hover→チャットに質問    送信 + 戦略を確定して伴走へ→
```
- URL/テキスト入力 → AB3C分析 → 結果表示
- URL分析時はウェブサイト改善レポートも同時自動生成
- 各セクションにhover時💬アイコン → クリックでチャットに質問送信
- チャットで深掘り → 「戦略を確定して伴走へ →」で確定

### 伴走フェーズ（STEP 2）
```
[サイドバー200px] [メインコンテンツ 1fr]         [アクションリスト 400px(fixed)]
  茶の濃い背景     戦略メッセージ（黒背景）         茶の薄い背景
  確定戦略         テーマタブ(18px)                アクション一覧
  スレッド一覧     ThreadChat                     クリック→詳細表示
                   初回自動アドバイス生成
```
- 確定戦略メッセージを黒背景で常時表示（分析タブと同じデザイン、Benefit/Advantageサブコメント付き）
- テーマ別チャット（全テーマ並列）
- テーマ初回選択時にAIが戦略ベースの初回アドバイスを自動生成

### デフォルトテーマ（伴走フェーズ）
| ID | ラベル | 初回アドバイス内容 |
|---|---|---|
| marketing | 集客・広告 | SEO/SNS/Googleマップ/プレスリリース/ネット広告/チラシの優先順位 |
| recruit | 採用コンテンツ企画 | ビジョン/強み/待遇案/キャリアプラン案を戦略から推論して提案 |
| website | ウェブサイト改善 | コンテンツ/デザイン/構造の改善優先事項 |
| subsidy | 補助金申請 | 小規模事業者持続化補助金の計画書項目に沿って概要書き出し |

## 料金体系（2026/04/15更新・税込・50%OFFキャンペーン中）

### プラン名称（2026/04/16変更）
- 旧「分析プラン」→ 旧「スポットプラン」→ **戦略診断チケット**
- 旧「伴走プラン」→ 旧「サブスクプラン」→ **戦略診断・策定・アクションプラン**（略称：**戦略指南サブスク**）
- タブ名: 旧「分析」→ 旧「戦略立案」→ **戦略策定**、旧「伴走」→ **戦略アクション**
- バッジ表示（check-pro/route.js）:
  - 戦略指南サブスク契約者 → `指南${契約サイト数}`（例: `指南1`, `指南5`, `指南15`）
  - 戦略診断チケット契約者 → `診断 ${残り回数}/${契約数}`（例: `診断 1/1`, `診断 99/100`）
    - 残り = site_limit - analyses_used（再分析もカウント）
  - pro_users直接登録 → `無制限`

### 戦略診断チケット（有効期限1年・チャットなし・ワンショット利用・履歴保存なし）
**仕様**: URLから一発分析でAB3C＋ウェブサイト改善レポート → PDF保存・印刷・シェアURL発行で持ち帰り。AIチャット機能なし、戦略アクションタブも利用不可（グレーアウト表示）。後日メール送信機能を追加予定。
- **有効期限**: 購入から1年間。購入したサイト数分の診断を1年以内に使い切る必要あり（未使用分は失効）
- **履歴保存なし**: 診断結果はlocalStorage含めて保存されない。ブラウザを閉じると消えるため、PDF/シェアURL/印刷で持ち帰る前提

| サイト数 | 定価（税込） | 1サイト単価 | 50%OFF価格 |
|---|---|---|---|
| 1サイト | ¥22,000/年 | ¥22,000 | ¥11,000/年 |
| 10サイト | ¥110,000/年 | ¥11,000（50%ボリュームディスカウント） | ¥55,000/年 |
| 100サイト | ¥770,000/年 | ¥7,700（65%ボリュームディスカウント） | ¥385,000/年 |

### 戦略診断・策定・アクションプラン（戦略指南サブスク・月額/年額・AIチャット付き・戦略アクション利用可・1サイト月100回上限）
**仕様**: 戦略診断チケットの全機能に加えて、AIチャットで戦略を磨く（戦略策定）、10施策テーマでアクションを検討する（戦略アクション）まで継続的に支援。「診断 → 策定 → アクション」の3段階すべてを含むため「戦略指南サブスク」。

**サイト枠（slot）の概念**:
- 30サイトプラン = **30個のサイトスロットを保持できる**（毎月新規30サイト分析できるという意味ではない）
- 一度登録したサイトは継続的に管理・運用（履歴・戦略確定・チャット履歴を保持）
- 不要になったサイトは削除可能。削除すると枠が空き、新規サイトを追加可能
- **サイト削除時、そのサイトの全データ（分析結果・チャット履歴・確定戦略）が削除される**

**月次更新（チャットチケットリセット）**:
- Stripeの `invoice.paid` イベント（`billing_reason !== 'subscription_create'`）で発火
- **強制リセット方式**: 残数に関わらず `サイト数 × 100` に強制リセット（前月の残数は失効）
- **契約日基準**（Stripeデフォルトのanniversary billing）: 4/17契約 → 5/17, 6/17... に課金＆リセット
- 次回更新日は user_plans.expires_at に保存、check-pro APIで取得しヘッダーに表示
  - 戦略指南サブスク: 「次回更新: 2026/5/17」
  - 戦略診断チケット: 「有効期限: 2026/4/17」
| サイト数 | 定価・月額（税込） | 定価・月額（税抜） | 50%OFF・月額 |
|---|---|---|---|
| 1サイト | ¥66,000 | ¥60,000 | ¥33,000 |
| 5サイト | ¥165,000 | ¥150,000 | ¥82,500 |
| 15サイト | ¥330,000 | ¥300,000 | ¥165,000 |
| 30サイト | ¥495,000 | ¥450,000 | ¥247,500 |
| 60サイト | ¥792,000 | ¥720,000 | ¥396,000 |
| 120サイト | ¥1,320,000 | ¥1,200,000 | ¥660,000 |

- 年額は月額×10（2ヶ月無料）
- 50%OFFキャンペーン: 4月30日までまたは100名到達まで。初回契約期間のみ適用、更新時は定価
- 3ヶ月返金保証あり
- **Stripe Price ID（2026/04/16現在 全て新価格で更新済み）**
- 無料トライアル: **24時間トライアル**（2026/08/16 実態を確認して記載を修正）
  - 初回 Google ログイン時に自動発行（`auth/[...nextauth]/route.js`）。`user_plans` に `plan_type='support'`・`site_limit=1`・24時間で失効、`tickets` に `remaining_chats=100`・24時間で失効
  - **分析は回数無制限**。`/api/usage` のトライアル判定は有効期限しか見ておらず、分析回数を数えていない（再分析も同様）
  - チャットは100回まで。再分析もチャット1枚を消費するが、実消費トークンは通常チャットの10倍以上ある
  - トライアル終了後は「無料ユーザー（永久1回）」の枠に落ちる
  - **回数・トークンの上限は意図的に設けていない**（2026/08/16 判断）。実際に大量消費した例が無く、体験を削る方を避けた。`api_usage` で監視し、問題が出てから塞ぐ方針

## APIルート

### コア機能
- `/api/analyze` — AB3C分析実行（Claude API、URL時はウェブ検索使用）
- `/api/chat` — チャット（通常/再分析/採用モード/テーマ別初回アドバイス）
- `/api/improve` — ウェブサイト改善レポート生成
- `/api/recruit` — 採用コンテンツ企画レポート生成（chatHistory対応）
- `/api/share` — 分析結果のシェアURL生成
- `/api/sites` — サイト登録・戦略確定の管理

### その他
- `/api/stripe/checkout`, `/api/stripe/webhook` — 決済
- `/api/check-pro` — PRO判定
- `/api/usage` — 使用状況
- `/api/contact` — コンタクトフォーム（現在console.logのみ、メール送信未実装）
- `/api/email/followup` — フォローメール
- `/api/user/purpose` — 利用目的（自社/代理店）
- `/api/admin/*` — 管理機能

### /api/chat の特殊モード
- `reanalyze: true` — チャット内容を反映した再分析
- `recruitMode: true` — 採用専門のヒアリングモード（戦略から推論したガイド型）
- `initialAdvice: true` — テーマ初回アドバイス自動生成（**チケット消費しない**）
- `threadTheme` — テーマ別コンテキスト付与

## API使用量の記録（app/lib/usage-log.js・2026/08/16 追加）

Anthropic を呼ぶ**9ルート14箇所すべて**で `logUsage()` を呼び、`api_usage` テーブルに1呼び出しずつ記録する。テーブルは初回利用時に自動作成（移行作業不要）。

- 記録内容: ユーザー・機能名・モデル・入力/出力/キャッシュ書込/キャッシュ読取の各トークン・ウェブ検索回数・概算コスト(USD)・サイトID・日時
- 機能名: `analyze` / `analyze_refine` / `reanalyze` / `chat` / `initial_advice` / `recruit_chat` / `chat_summary` / `chat_continuation` / `action_summary` / `improve` / `improve_visual` / `recruit` / `rakuten_*` / `web_update_*`
- **記録の失敗は本処理を壊さない**（例外は握りつぶしてログのみ）。分析成功後に記録DBエラーでユーザーがエラー画面を見ることを防ぐ
- **DB接続は遅延初期化**。モジュールスコープで `neon()` を呼ぶと `DATABASE_URL` の無いビルド環境で `next build` が落ちる
- 単価改定時は `usage-log.js` 冒頭の `PRICING` 定数のみ修正

操作1回あたりの原価を出すクエリ（ポイント設計・課金設計の根拠に使う）:
```sql
SELECT feature, COUNT(*) AS 回数,
       ROUND(AVG(estimated_cost_usd)::numeric, 4) AS 平均ドル,
       ROUND(AVG(input_tokens + cache_creation_tokens + cache_read_tokens)) AS 平均入力トークン
FROM api_usage GROUP BY 1 ORDER BY 平均ドル DESC;
```

**背景**: 2026/08/16 に API 使用量の急増を調査した際、操作別の原価を測る手段が無く特定に丸一日かかった。原因は不正利用ではなく、無制限権限を付与したモニター・研修参加者の集中利用だった。`key_4` は本番キー（停止禁止）。

## コンポーネント構成（app/page.js）

### 共通コンポーネント
- `ChatBtn` — hover時のみ表示される💬アイコン（phase1色背景・白SVG、position:absolute）
- `hoverShow` — hover時にChatBtnを表示するイベントハンドラ（:scope >で直接子のみ）
- `Card` — 白背景カード（onChat対応、hover時ChatBtn表示）
- `UL` — リスト（各アイテムにonChatItem対応）
- `SectionLabel` — B/A/3Cセクション見出し（onChat対応）
- `ResultView` — AB3C分析結果の全セクション表示（onChat propで全レベル💬対応）
- `Badge` — チェックポイントのok/warn/ngアイコン

### チャットコンポーネント
- `AnalysisChatPanel` — 分析フェーズ用（再分析機能・トピック送信・戦略確定ボタン付き）
- `ThreadChat` — 伴走フェーズ用（テーマ別・アクション登録・採用レポート生成・初回自動生成対応）

## メール実装状況（Resend）
- **実装済み（2026/04/16）**: app/lib/email.js に全9種の関数あり（本文HTML確定済み）
  - sendRegistrationEmail（登録完了）
  - sendWelcomeEmail / sendWelcomeEmailAgency（自社/代理店ウェルカム）
  - sendAnalysisCompleteEmail（分析完了）
  - sendFollowUpEmail（チュートリアル#1: シェアURL活用、3日後）
  - sendTutorial2Email（#2: アクション相談、5日後）
  - sendTutorial3Email（#3: NotebookLM、8日後）
  - sendTutorial4Email（#4: 補助金申請、12日後）
  - sendTutorial5Email（#5: 毎日のアクション習慣、25日後）
- **配信スケジュール（2026/04/16）**: `/api/email/cron-tutorial` で全#1〜#5を一括処理
  - Vercel Cron で UTC 0:00（JST 9:00）に毎日実行
  - email_logs テーブルで二重送信防止
  - 認証: Authorization: Bearer ${CRON_SECRET}（Vercel自動付与）または x-cron-secret
  - 旧 /api/email/followup は新エンドポイントに転送（後方互換）
- FROM_EMAIL=info@senryaku.ai（Resendドメイン認証 senryaku.ai 完了済み）
- TODO: メール本文への画像（スクリーンショット）追加は別途対応

## 開発予定機能・要件

### ✅ 2026/04/16セッションで完了した変更
- **プラン名再設計**: スポット→**戦略診断チケット**、サブスク→**戦略診断・策定・アクションプラン**（略称：戦略指南サブスク）
- **「立案」→「策定」統一**: タブ名「戦略立案」→「戦略策定」、説明文も全て更新
- **戦略診断チケット=チャットなしワンショット化**: AnalysisChatPanelをチャット権限ある人だけに表示、CTAバナー追加
- ~~**トライアルチャット廃止**: 新規ユーザーへの`is_trial=true`チケット付与を削除~~ → **現在は事実と異なる**。`auth/[...nextauth]/route.js` は今も24時間トライアル（`is_trial=true` のチケット100枚＋`user_plans`）を発行している。上の「料金体系」の記載が実態（2026/08/16 確認）
- **戦略診断チケット価格改定**: 1サイト¥44,000→¥22,000、10サイト¥110,000、100サイト¥770,000
- **Stripe Price ID更新**: 戦略診断チケット3つ + 戦略指南サブスク12個すべて新価格で作成済み
- **50%OFFクーポン**: Stripeで`DAWbDiCg`作成済み、Vercel環境変数`STRIPE_CAMPAIGN_COUPON_ID`に設定済み
- **「年間ライセンス」→「有効期限1年」**: 履歴保存なし・1年以内使い切り表記を追加
- **DB保存バグ修正**: PostgreSQL型キャスト追加で「分析結果が未分析のまま」問題解消
- **印刷レイアウト修正**: 右余白・空白ページ・幅問題を修正、PDFファイル名にサイト名+戦略メッセージ
- **新規分析時のリセット強化**: history/threads/localStorageを完全クリア
- **PROバッジ→「無制限」**に変更
- **howto/page.js全面見直し**: 「戦略指南 AIとは」追加、3段階の使い方、AB3C分析結果の見方
- **戦略確定ボタンを結果表示エリアに追加**（チャットなしユーザー向けにグレーアウト対応）

### 未解決バグ（要確認）
1. **戦略アクションタブのテーマ切替が正しく動作しない（修正済み・要確認）**
   - 症状: 採用コンテンツ企画を選んでも集客・広告のアドバイスが表示される
   - 修正内容: `key={activeThreadId}`再マウント、`initialized` ref、AbortController追加
   - 残りの確認事項: 本番で「全リセット」→各テーマ選択→正しいアドバイスが表示されるかテスト

2. **右カラムの位置調整**
   - sticky top:130px / height:calc(100vh-130px) に設定済み
   - 下部のボタン見切れは要確認

### セミナー前の最終確認（2026/04/22 セミナー時点のチェックリスト・履歴として残置）
- [ ] 本番環境（senryaku.ai）で新プラン名が正しく表示されているか
- [ ] 戦略診断チケット購入フローが新Price IDで動作するか（テストモード）
- [ ] 戦略指南サブスク購入フローが動作するか
- [ ] 50%OFFクーポンが自動適用されるか
- [ ] PROユーザー（権さん）でフル機能が使えるか
- [ ] トライアルユーザーで戦略診断チケット体験ができるか（チャット非表示確認）

### 構想・GO待ち（着手前に権さんの判断が必要）
実装手順に飛ばず、権さんが「十分検討できた」と判断して GO を出すまで検討を重ねること。
- **ログインが必要なサイトの分析** — **現状は不可**。`fetchWebsite()`（`app/api/analyze/route.js`）は Cookie もセッションも持たない素の GET を1回投げるだけで、認証情報を渡す口がない。しかも**ログイン壁を検知していない**ため、ログインページのHTMLが取れた場合はそれを「サイトの中身」として分析してしまう（エラーにならない分こちらが危険）。会員制サイト・ステージング環境（Basic認証）・EC管理画面側などで需要が出る想定
  - 最小の一歩: **ログイン壁の検知と警告**。フォーム要素・タイトル・本文量から「ログインページを取得した可能性が高い」と判定し、分析を止めて案内する。これだけでも誤分析を防げる
  - 回避策（実装ほぼ不要）: ログイン後のページを手元でコピーし、テキスト入力タブ／チャットの `.txt` 添付で渡してもらう。まずここに誘導する運用でしのげるか検討
  - 中間案: Basic認証のみ、その場限りで ID/PW を受け取りヘッダー付与（保存しない）。ステージング確認の需要には効く
  - 重い案: ヘッドレスブラウザ（Playwright等）＋認証情報の預かり。**顧客の資格情報を預かる責任が跳ね上がる**ため、原則避ける方向で検討する
  - **設計の境界線（2026-08-14 議論）**: 「**ログインはユーザーが行う。AI・当社は読むだけ**」。この線を越えて認証を代行しようとした瞬間に、責任も実装も跳ね上がる
    - AIにID/PWを入力させる案は不可。Claude in Chrome 等は「エンドユーザーが自分のClaudeで使う機能」であり、Vercel上の当社アプリから顧客のブラウザを操作することはできない。認証情報の入力もAIが代行する前提の設計にはなっていない
    - **本命は「ログイン済みブラウザから中身を渡す」方向**。①ブックマークレット／ブラウザ拡張でDOMを取得して senryaku.ai に送る（当社単独で作れる・認証情報に触れない）②ユーザー自身のClaude（Code/Desktop/Chrome）がログイン済みページを読み、MCP経由で投げ込む ③手動コピペ（現状）
    - ②は **MCPサーバー化と重なる**。ただしフェーズ1は読み取りツールのみのため、「外部から本文を投げ込む書き込み口」を別途足す必要がある
  - **ブックマークレット案の射程（2026-08-14 議論）**: 素の実装では「開いているページ1枚」だが、**同一ドメインのリンクを辿る巡回**と**構造情報の保持**まで作り込める。ログイン済みCookieが自動で付くため会員ページも続けて取れる
    - **これはログイン対応を超える論点**。現状の `extractText()` はタグを全て剥ぎ取り**先頭5000字で打ち切った素のテキスト**しかAIに渡していない（見出し階層・ナビ・パンくず・ページ間リンクは捨てている）。巡回＋構造保持を入れると**分析入力そのものの質が上がる**（特にウェブサイト改善レポートは現状「構造を見ずに」書いている）。ログイン対応はその副産物という位置づけになり得る
    - 巡回の制約: タブを開いたまま待たせる／JavaScriptで画面を組むサイト（ログイン後の管理画面に多い）は中身が空で返る／「同一ドメイン・最大◯ページ・階層◯まで」の上限設計が必須／相手サーバーへの負荷配慮
    - トレードオフ: 入力が増えれば生成時間・トークンコストが上がる。どこまで渡すかは要設計
  - 未確定: そもそもどの需要（会員制サイト／ステージング／管理画面）に応えるのが本筋か
- **FutureShop へのホワイトラベル提供** — 以前 FutureShop さんから「ホワイトラベルで戦略指南を提供できないか」と打診を受けている（打診レベル・条件詰めはこれから）。論点は**どこまでをホワイトラベルにするか**の段階設計:
  1. **書き出し結果のみ**（最も軽い）… 書き出し結果からロゴを抜くことは既に可能（権さん談。着手時に実装箇所を確認すること）。ただし AB3C 分析を学ぶ時点で当社と紐づくので、完全な無名化にはならない前提
  2. **FutureShop ブランドのページを作って展開**… 分析入口を先方ブランドで持つ
  3. **FutureShop の機能内に分析機能を取り込む**（最も深い）… 先方サービスの一機能として組み込む
  - **MCPサーバー化（中優先度タスク）と論点が重なる**。フェーズ1のAPIキー認証層がそのまま組み込みの土台になるため、両者を合わせて一度実装を検討する
  - 未確定: どの段階から始めるか、AB3C の帰属表示をどう扱うか（ブランド価値と無名化のトレードオフ）、課金・レベニューシェアの形（既存の特別提携プランとの関係）
- **集客版戦略指南（強み起点のコラム量産で検索・SNS・AI経由の集客をつくる）** — 2026-08-14構想。採用版と対になる「集客」側の入口。自分たちの強みをベースにしたノウハウのコラムをたくさん書いていくことで、検索エンジン・SNS、そして AI（生成AIの回答／AI検索）からの集客をつくる**コンテンツ生成の仕組み**。これも単独のサービスとして切り出せないかを検討する
  - 裏側は同じく戦略指南が動く。強み・ベネフィット・アドバンテージを戦略的に分析した上で「こういうコンテンツを作っていったらどうでしょう」を提案する構成
  - AB3C原則の「AB3C→ユーザーシナリオ→CJM→コンテンツ」の順を踏襲。コンテンツ企画フロー（コンテンツ比較表・プッシュ/プル設計・ユーザーモデル別マッピング）は `docs/site-generation-design-flow.md` が正典
  - 既存資産の流用: 戦略アクションの `marketing`（集客・広告）・`website` テーマ、Web系アクションテーマ群
  - 呼び方は「LLMO」「AIO」いずれの語も当てはまるが、**ユーザー向け表示では英略語を出さない**（言語ポリシー準拠）。表向きの名乗り方は未確定
  - 未確定: 単発のコラム生成に留めるか継続運用（本数計画・更新）まで持つか、1本あたりの品質担保（一般論の量産にしない歯止め）、課金の扱い
- **採用版戦略指南（採用ページ書き出しを入口にした戦略指南）** — 2026-08-14構想。企業がウェブでやりたいことは突き詰めると**集客か採用**の二つ。ところが「まず戦略を全体で固めましょう」は入口として重く、腰が引ける。そこで**表向きは「採用ページを作るサービス」**として軽く見せ、**裏側では戦略指南が動いて、採用コンテンツのベースとなる企業の戦略をまず作る**。その上で採用ページを書き出す、という構成にしたい（今回の着手対象は採用。集客側は別途）
  - 出口は**採用ページの生成まで含めてよい**（ページを作り切る前提で設計する）。ビジュアル実装は Claude Design に渡す方針を踏襲、生成フローの正典は `docs/site-generation-design-flow.md`
  - 既存資産の流用: 戦略アクションの `recruit` テーマ・`/api/recruit`（採用コンテンツ企画レポート）、Web系アクションテーマ群
  - 新規事業版と同じく**フロントのみ切り分け・分析エンジンは共通**の型で考える
  - 未確定: 戦略を「作らせている」ことをどこまで表に出すか（軽さを損なわない見せ方）、専用ページを別導線にするか既存フロー内のモードにするか、課金の扱い
- **新規事業向け戦略指南（社名生成＋戦略ナラティブ）** — 2026-07-25構想。フロントのみ切り分け・分析エンジンは共通。社名＝戦略メッセージの圧縮なので nazuke ではなく戦略指南側で持つ。ナラティブ（500〜1000字の一人語り）は引き出す対話が最難関
- **楽天市場・商品分析版 v1** — 分析単位を店舗→商品LPへ。正典は `docs/楽天版-仕様書-v1.md`。レビュー取得スパイクは成功済み、実装はGO待ち
- **クレジット課金モデル** — サイト数廃止・全機能クレジット統合。設計完了（分析+改善レポート=100cr / チャット=1cr / 初回アドバイス=0cr）。戦略アクションのWeb系絞り込み変更と合わせて実装予定
- **自己分析専用ページ（価値観深掘りの本格版）** — 簡易版は既存チャットに統合済み。本格版は着手時に手順詳細を権さんへ再確認
- **提案書の業界別バージョン（税理士版・EC版）** — 方針合意済み、着手は後日

### 実装予定タスク（優先度高）
- **分析入力の拡充（最優先・2026-08-20 合意）** — 現状の分析入力は「指定URL1ページ＋5000字カット」（`extractText()` の `.slice(0, 5000)`）だけで、下層ページの内容はウェブ検索経由で偶然入っているに過ぎない（制御不能・毎回変わる）。改善レポート（`/api/improve`）に至ってはサイトを一切見ていない（URLの文字列と分析JSONのみ）。これを意図した設計に置き換える:
  - 主要ページの直接取得（トップからのリンクを辿って数ページ〜、同一ドメイン・上限設計必須）
  - 字数上限の見直し（5000字カットは base64画像混入対策として2026-03-30に3000→5000にした経緯。ページ単位の上限に再設計）
  - 改善レポートに実ページのテキスト・構造を渡す（現状「構造を見ずに」構造改善を提案している）
  - 経緯: URL対応初日（2026-03-19）の最小実装が残ったもの。同日追加のウェブ検索が下層内容を拾って補っていたため不足が表面化しなかった
  - **画像・スクリーンショットの読み込みは不要（2026-08-20 権さん判断）**: 改善レポートのデザイン項目は「新戦略に即したサイトはこうあるべき」を示すもので、現状デザインの批評ではない＝ビフォーの見た目は入力に要らない。唯一の利用価値だったカラーパレット抽出は、クロール時にCSS（現状捨てている `<style>`・外部CSSファイル）から色コードをテキストとして拾えば足りる。配色ルールは「ロゴカラーから濃淡2色」（[[rule-web-color-from-logo]]・2026-06-14）と整合
- **分析結果の拡張書き出し** — チャットで質問すると返ってくるような各項目の詳細を、最初からまとめて書き出す機能。2段階で検討済み:
  - A. 「詳細を一括展開」ボタン（**まずこちらを試す**）… 分析完了後にボタン1つで全セクションの💬を自動実行しインライン表示。既存 `/api/chat` を再利用でき実装が早い
  - B. 分析自体を詳細版で生成する別モード（最終形）… `/api/analyze` の出力を拡張。PDFにも詳細が入るが生成時間・トークンコスト増
  - まずAで見た目を確認してからBへ移行するか判断する、が合意済み方針
- **拡張レポートのmdファイル書き出し** — 上記の拡張レポートまでを md でダウンロードし、Claude に読み込ませて使えるようにする。PDF・シェアURL・印刷に並ぶ「第4の持ち出し手段」。チャット側の `.txt` 添付（実装済み）の逆方向。未確定: 改善レポート・チャット履歴・確定戦略まで含めるか／チケット消費の扱い
- **戦略診断結果のメール送信** — 分析完了時にPDFリンクまたはHTML本文をResendで送信。FROM: info@senryaku.ai。テンプレートに戦略指南サブスク誘導CTA入れる
- **ダッシュボードの戦略診断ユーザー専用UI** — 現在は全ユーザーに同じサイトリストを表示している。戦略診断チケットユーザー向けに「残り◯サイト」のシンプル表示に切り替える
- **戦略診断チケット＋戦略指南サブスク両方所持時の扱い整備** — 2026/04/18時点で暫定処置として「戦略指南サブスク優先表示」（check-pro/route.js の ORDER BY で plan_type='support' を優先）で対応。正しい対応は以下いずれか:
  1. 戦略指南サブスク購入時、既存の戦略診断チケットを自動キャンセル＆日割り返金（Stripe側も）
  2. 戦略診断チケットユーザーが戦略指南サブスク購入ボタンを押した時点で「アップグレード確認」モーダル表示
  3. 両方購入は原則禁止（既に片方を持っていればもう一方のボタンを無効化）
  - 現状の暫定処置: 診断チケットはactiveのままDBに温存されるが、バッジ・機能は戦略指南サブスク扱い。ユーザーが損することはないが、診断チケットの枠が塩漬けになる。
- **決済跨ぎの分析結果保持** — 現状、無料トライアル中に分析した結果は決済画面（Stripe）に遷移して戻ると消える。sessionStorage等で一時保存→復元する仕組みが必要。または決済前に「PDF保存・シェアURL発行で持ち帰ってください」の警告表示。2026/04/18のテストで発覚。
- **戦略診断チケットのバッジ表示統一** — ✅ 実装済み（2026/04/20）: 常に「診断 残り/総数」形式に統一（1サイトも「診断 1/1」）。さらに analyses_used カウンタに基づき再分析も消費反映。
- **チャット履歴・戦略確定履歴のDB保存** — ✅ 実装済み: sites テーブルに `confirmations` / `threads` / `theme_chats` / `analysis_chat` を JSONB で保存（`app/api/sites/route.js`）

### 実装予定タスク（中優先度）
- **MCPサーバー化（フェーズ1＋2まで実装で合意・2026-08-14）** — ユーザーが自分のClaude（Claude Code / Desktop）に戦略指南AIを接続し、保存済みデータの参照＋戦略相談ができるようにする。シェアURLの「書き出し分量が少ない」問題もDBのJSONB全文を返すことで根本解消
  - フェーズ1（実働2〜3日）: APIキー発行UI＋キーテーブル、Next.jsルートにMCPエンドポイント（Vercel公式アダプタ）、読み取りツール4本（サイト一覧・AB3C分析全文・確定戦略・改善レポート）
  - フェーズ2（＋1〜2日）: 相談ツール。外部AIから「戦略指南に聞く」を呼べる。裏側は既存 /api/chat ロジック流用、チャットチケット消費
  - フェーズ3（claude.aiカスタムコネクタ用OAuth対応）は需要を見てから。今回スコープ外
  - FutureShop型ホワイトラベル（REST API提携組み込み）は別線だが、フェーズ1のAPIキー認証層が土台として流用できる
  - 着手前に権さんと決める論点3つ: ①公開範囲（チャット履歴まで全部か、分析結果＋確定戦略までか）②対象プラン（戦略指南サブスク限定特典にするか）③相談ツールのチケット消費（UIチャットと同一カウントか）
- **スマホ対応（レスポンシブ対応）** — 現状はPC/デスクトップ幅前提のレイアウト。サイドバー200px固定・右カラム400px固定の3カラム構造、ヘッダー、分析結果カード、印刷レイアウトなどをモバイル幅で破綻なく表示できるよう調整。分析フェーズ・伴走フェーズ両方で動作確認が必要。既存の色・フォントサイズルール（本文最低18px）を維持しつつ、タブレット/スマホでのナビゲーション方式（サイドバー折りたたみ・ボトムシート等）を検討。
- **ターゲット絞り込み機能（ユーザーモデル別分析）** — 3C分析のCustomerで複数のターゲット種別が出た際、特定のユーザーモデルに絞り込んで再分析する機能。絞り込むと市場規模は小さくなるが、年商10億未満の中小企業には十分な規模であり、戦略が強く成立しやすくなる。ターゲット種別の横に「このユーザーに絞り込む」ボタンを設置し、絞り込んだAB3C分析を再生成するイメージ。
- **テーマ初回アドバイスの動作確認・改善**
- **採用コンテンツ企画の改善** — AIが戦略分析から推論した提案をベースにガイド（一問一答ではない）
  - Advantageが弱い場合は待遇面の差別化をアドバイス
  - ビジョンを戦略メッセージから提案して確認
  - 同業界のキャリアパスを提示して比較
  - 独立支援など特有の強みを求職者向けに言い換え
- **分析レポートの日本語ラベル統一** — AI出力・UI表示に残る `warn` / `ng` 等の英単語を「注意」「警告」等の日本語へ。コード側の未修正箇所を洗い出して直す
- **OpenAI画像生成APIをウェブサイト生成フローに組み込み** — 装飾・背景画像はAPIで自動生成、人物写真は手動。APIキー取得後に着手（**ウェブサイト生成の話題が出たら必ず提示すること**）
- **ハイライト表示のDB化** — `changedPaths` が React state のみのためリロードで消える
- **チュートリアルメール実装** — ✅ 実装済み（#1〜#5 を `/api/email/cron-tutorial` で日次配信、email_logs で二重送信防止）

### 実装予定タスク（低優先度）
- **frontend-designプラグイン導入** — Anthropic公式のClaude Codeプラグイン。既存のカラールール（赤=Benefit、青=Advantage等）との整合性を確認してから導入すること。GitHub: https://github.com/anthropics/claude-code/tree/main/plugins/frontend-design
- **Google Ads API（Keyword Planner）統合** — SEOチャットにキーワード検索ボリューム表示。Google Adsアカウント作成済み、開発者トークン申請は未完了
- **スクロール時のヘッダー高さ変動（ジャンプ）修正**
- **ケーススタディギャラリーページ（/examples）の実装**
- **セキュリティ脆弱性対応**（Next.js 14.2.5、Dependabot alerts: 3 critical / 14 high / 15 moderate / 2 low）
- **コンタクトフォームのメール送信実装**（現在console.logのみ）

### 各テーマのチャット→アクション化フロー
全テーマ共通の流れ:
1. テーマ選択 → AIが戦略ベースの初回アドバイスを自動生成
2. ユーザーと会話しながら具体化
3. AIが具体的なアクションを提案時に `[ACTION: タイトル]` 形式で明示
4. 「アクションに登録」ボタン → 右カラムのアクションリストに追加
5. アクションリストで管理（クリックで詳細表示・削除可能）

### 採用コンテンツ企画の改善要件
- AIが戦略分析から推論した提案をベースにガイド（一問一答ではない）
  - Advantageが弱い場合は待遇面の差別化をアドバイス
  - ビジョンを戦略メッセージから提案して確認
  - 同業界のキャリアパスを提示して比較
  - 独立支援など特有の強みを求職者向けに言い換え

## デプロイ
- mainブランチへのpushでVercel自動デプロイ
- ビルドエラー（neon DB接続）はCI環境のenv未設定が原因。"Compiled successfully" が出ていればOK
- 現在のワークツリー: `.claude/worktrees/stupefied-sammet`
- Vercel環境変数変更後は手動再デプロイ必要。新規コミットのpushが最も確実

## 技術的な重要教訓
- `.env.local`等の秘密情報をコミットした場合: `git reset --soft HEAD~1` → ファイル除去 → 再コミット（force pushは不可）
- `str_replace`ツールは大きなファイルで文脈が曖昧だと失敗する。十分にユニークな周辺コードを検索文字列に含める
- `page.js`にインライン記述されたFooterコードはFooter.jsとは別物（ページ固有のインラインコードを先に確認する習慣）
- `page.js` は巨大ファイル（1400行超）。読み込み時はoffset/limitを使用

## 注意事項
- localStorageキー: `ab3c_history`, `ab3c_threads_${siteId}`, `ab3c_thread_${threadId}`, `ab3c_actions_${siteId}`, `ab3c_chat_summaries`
- Header.js, PricingModal.js は変更対象外（指示がない限り）
- 認証・決済フローは変更しない
- 権さんが言っていない発言をClaudeが誤って帰属させないこと（注意）
- UIは反復的なフィードバックで調整（色・サイズを複数ラウンドで詰める傾向）
- 実装前に戦略的優先順位を明示し、合意を取ってから作業開始
